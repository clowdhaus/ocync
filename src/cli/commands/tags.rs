pub async fn run(
    _repo: &str,
    _glob: Option<String>,
    _semver: Option<String>,
    _exclude: Option<String>,
    _sort: Option<String>,
    _latest: Option<usize>,
) -> i32 {
    eprintln!("tags: not yet implemented");
    1
}
