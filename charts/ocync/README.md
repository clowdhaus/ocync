# ocync Helm chart

Deploy ocync on Kubernetes as a Deployment (watch mode), CronJob, or Job.

```bash
helm install ocync oci://public.ecr.aws/clowdhaus/ocync --version 0.1.0
```

See the [Helm chart documentation](https://clowdhaus.github.io/ocync/helm) for configuration and deployment modes.
