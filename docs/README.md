# docs

ocync documentation site built with [Astro](https://astro.build/).

## Development

```bash
cd docs
npm install
npm run dev      # Start dev server at http://localhost:4321
npm run build    # Build static site to dist/
npm run preview  # Preview built site locally
```

## Deployment

Deployed to GitHub Pages via the `docs.yml` workflow on push to `main`.
